apiVersion: v1
kind: Service
metadata:
  name: spring-boot-k8s-gitops-flux-sops
  labels:
    app.kubernetes.io/name: spring-boot-k8s-gitops-flux-sops
spec:
  selector:
    app.kubernetes.io/name: spring-boot-k8s-gitops-flux-sops
  ports:
    - name: http
      port: 80
      targetPort: 8080
