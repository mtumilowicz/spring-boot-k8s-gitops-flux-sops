apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-k8s-gitops-flux-sops-workshop
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: spring-boot-k8s-gitops-flux-sops-workshop
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev
